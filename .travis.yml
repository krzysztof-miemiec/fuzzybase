sudo: false
language: node_js
node_js:
- 10

cache:
  yarn: true
  directories:
  - node_modules
  - app/node_modules

install:
- yarn
- cd app && yarn && cd ..

script:
- node --version
- yarn lint
- yarn test
- yarn lemmy

branches:
  only:
  - develop
  - master
