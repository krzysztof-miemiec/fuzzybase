import { createStyles } from '../../../../app.styles';

export const styles = createStyles({
  container: {
    display: 'flex',
    flexFlow: 'row nowrap',
  },
  input: {
    flex: '1 0 auto',
    maxHeight: 200,
    display: 'grid',
    background: 'transparent',
    fontSize: 13,
  },
});
