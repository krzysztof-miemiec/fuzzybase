import { R } from '../../../../../common/resources';
import { createStyles } from '../../../../app.styles';

export const styles = createStyles({
  button: {
    flex: '0 0 auto',
    margin: 8,
    fontSize: 20,
    height: 48,
    display: 'flex',
    alignItems: 'flex-end',
    color: R.color.gray,
  },
});
